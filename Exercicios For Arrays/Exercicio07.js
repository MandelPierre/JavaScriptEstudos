//Utilize o método forEach para imprimir cada elemento de um array juntamente com seu índice.
const num = [5, 3, 9, 12, 0, 8];

num.forEach((numero, indice) => {
    console.log(`O número do índice ${indice} é: ${numero}`);
});